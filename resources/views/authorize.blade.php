<div class="container">
    <h1>Authorization</h1>

    <button class="btn btn-success">Authorize</button>

    {{ $client }}
    <!-- ## return data ##
    'client' => $client,
    'user' => $user,
    'scopes' => $scopes,
    'request' => $request,
    'authToken' => $authToken, -->
</div>